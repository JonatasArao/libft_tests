{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "if [ \"$(basename ${fileDirname})\" == \"tests\" ]; then make debug TEST=$(echo ${fileBasenameNoExtension} | sed 's/^test_//'); else make debug TARGET=$(dirname ${relativeFileDirname}) TEST=${fileBasenameNoExtension}; fi",
            "group": "build",
            "problemMatcher": "$gcc"
        }
    ]
}
